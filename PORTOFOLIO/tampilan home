<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>NASA Futuristic Minimalist Design</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <style>
    /* Global styling */
    body {
      margin: 0;
      padding: 0;
      background: #fff;
      font-family: 'Roboto', sans-serif;
      height: 100vh;
      overflow: hidden;
      position: relative;
    }

    /* Center SVG container */
    .center-svg {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    /* Curved text styling */
    .curved-text {
      font-size: 16px;
      font-weight: 700;
      dominant-baseline: middle;
      text-anchor: middle;
    }

    /* Decorative dots */
    .dot {
      fill: #000;
    }

    /* Labels near dots */
    .label {
      font-size: 10px;
      fill: #000;
    }
  </style>
</head>
<body>
  <div class="center-svg">
    <svg width="400" height="400" viewBox="0 0 400 400">
      <!-- Define paths for curved text -->
      <defs>
        <path id="outerPath" d="M50,200 a150,150 0 1,1 300,0 a150,150 0 1,1 -300,0" />
        <path id="innerPath" d="M100,200 a100,100 0 1,1 200,0 a100,100 0 1,1 -200,0" />
      </defs>

      <!-- Concentric circles -->
      <circle cx="200" cy="200" r="150" stroke="#000" stroke-width="2" fill="none" />
      <circle cx="200" cy="200" r="100" stroke="#000" stroke-width="2" fill="none" />

      <!-- Curved text on outer circle -->
      <text class="curved-text">
        <textPath href="#outerPath" startOffset="10%" method="align" spacing="auto">COLLABORATION</textPath>
      </text>

      <!-- Curved text on inner circle -->
      <text class="curved-text">
        <textPath href="#innerPath" startOffset="50%" method="align" spacing="auto">NASA</textPath>
      </text>

      <!-- Dot dan label on outer circle (moved further from curved text) -->
      <circle class="dot" cx="70" cy="275" r="3" />
      <text x="50" y="295" class="label">AI. Yadin</text>

      <!-- Additional dot on outer circle -->
      <circle class="dot" cx="317" cy="294" r="3" />

      <!-- Dot on inner circle -->
      <circle class="dot" cx="121" cy="139" r="3" />
    </svg>
  </div>
</body>
</html>
